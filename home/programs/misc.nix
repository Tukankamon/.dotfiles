{
  ...
}: {
  programs.vscode = { # Just here for the stylix theme
    enable = false; 
  };

  programs.obsidian = {
    enable = false; 
  };

  programs.spotify-player = {
    enable = true;
  };
}